format = """$hostname$directory$git_branch$git_state$git_status$cmd_duration$python$fill$memory_usage$line_break$status$character"""

[hostname]
ssh_only = true
format = '[ $hostname ](inverted bold)'
disabled = false

[directory]
style = "inverted bold"
format = '[ $path ]($style)'
truncation_length = 4
truncation_symbol = '…/'
truncate_to_repo = true
home_symbol = '~'

[git_branch]
format = ' [$branch]($style)'
style = "dimmed"

[git_status]
format = '([$all_status$ahead_behind]($style))'
style = "bold"
conflicted = "~"
untracked = "?"
modified = "!"
staged = "+"
renamed = "»"
deleted = "✘"
stashed = "≡"
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕${ahead_count}⇣${behind_count}"

[git_state]
format = ' \([$state( $progress_current/$progress_total)]($style)\) '
style = "dimmed"

[character]
success_symbol = "[❯](bold)"
error_symbol = "[❯](inverted bold)"
vimcmd_symbol = "[❮](bold)"
vimcmd_replace_one_symbol = "[❮](bold)"
vimcmd_replace_symbol = "[❮](bold)"
vimcmd_visual_symbol = "[❮](bold)"

[cmd_duration]
format = " [$duration]($style)"
style = "dimmed"
min_time = 500

[python]
format = " [$virtualenv]($style)"
style = "dimmed"

[fill]
symbol = " "

[memory_usage]
format = '$symbol[${ram_pct}]($style)'
disabled = false
threshold = -1
symbol = '  '
style = 'dimmed'

[status]
style = 'dimmed'
symbol = ''
success_symbol = ''
format = '[$common_meaning$signal_name$maybe_int]($style) '
map_symbol = true
disabled = false

[username]
disabled = true
